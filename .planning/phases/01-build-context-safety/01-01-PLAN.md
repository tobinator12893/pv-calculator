---
phase: 01-build-context-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pv-calculator/client/.dockerignore
  - pv-calculator/server/.dockerignore
autonomous: true
requirements:
  - BUILD-03
must_haves:
  truths:
    - "docker build for the client does not send node_modules, dist, .env, or .git to the daemon"
    - "docker build for the server does not send node_modules, dist, .env, or .git to the daemon"
    - ".dockerignore files exist at pv-calculator/client/ and pv-calculator/server/"
  artifacts:
    - path: "pv-calculator/client/.dockerignore"
      provides: "Build context exclusion rules for Vite/React client"
      contains: "node_modules"
    - path: "pv-calculator/server/.dockerignore"
      provides: "Build context exclusion rules for Express/TypeScript server"
      contains: "node_modules"
  key_links:
    - from: "pv-calculator/client/.dockerignore"
      to: "docker build pv-calculator/client/"
      via: "Docker CLI reads .dockerignore from build context root before sending context to daemon"
      pattern: "\\.dockerignore"
    - from: "pv-calculator/server/.dockerignore"
      to: "docker build pv-calculator/server/"
      via: "Docker CLI reads .dockerignore from build context root before sending context to daemon"
      pattern: "\\.dockerignore"
---

<objective>
Create .dockerignore files for both client and server containers to ensure Docker build contexts are minimal, secure, and free of secrets.

Purpose: Prevent node_modules (hundreds of MB), dist (stale build artifacts), .env (contains real Cesium Ion API token), and .git from being sent to the Docker daemon during builds. This is a prerequisite for all subsequent Dockerfile phases.
Output: Two .dockerignore files at pv-calculator/client/.dockerignore and pv-calculator/server/.dockerignore
</objective>

<execution_context>
@/Users/tobiashuber/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tobiashuber/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-context-safety/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .dockerignore files for client and server</name>
  <files>pv-calculator/client/.dockerignore, pv-calculator/server/.dockerignore</files>
  <action>
Create two .dockerignore files with explicit exclusion patterns. Both files follow the same structure but are placed independently per the per-service pattern documented in the research.

**pv-calculator/client/.dockerignore** must exclude:
- `node_modules` and `**/node_modules` (dependencies rebuilt inside container)
- `dist` (build output produced by Vite inside container; also large due to Cesium assets)
- `.env` and `.env.*` with `!.env.example` negation (client has a real .env with VITE_CESIUM_ION_TOKEN)
- `.git` and `.gitignore` (git metadata not needed in image)
- `.DS_Store`, `.vscode`, `*.local` (editor/OS artifacts)
- `*.log`, `npm-debug.log*` (logs)
- `*.tsbuildinfo` (TypeScript build info)

**pv-calculator/server/.dockerignore** must exclude the same categories:
- `node_modules` and `**/node_modules`
- `dist` (compiled TypeScript output)
- `.env` and `.env.*` with `!.env.example` negation (proactive -- no .env exists yet but prevents future leaks)
- `.git` and `.gitignore`
- `.DS_Store`, `.vscode`, `*.local`
- `*.log`, `npm-debug.log*`
- `*.tsbuildinfo`

Use the exact patterns from the research (Code Examples section). Each file should have clear comment headers explaining each exclusion category. Do NOT exclude `Dockerfile` (misleading and unnecessary since Docker sends it regardless).
  </action>
  <verify>
    <automated>cd /Users/tobiashuber/Desktop/Claude/pv-calculator/client && test -f .dockerignore && grep -q "node_modules" .dockerignore && grep -q ".env" .dockerignore && grep -q "dist" .dockerignore && grep -q ".git" .dockerignore && echo "client OK" && cd /Users/tobiashuber/Desktop/Claude/pv-calculator/server && test -f .dockerignore && grep -q "node_modules" .dockerignore && grep -q ".env" .dockerignore && grep -q "dist" .dockerignore && grep -q ".git" .dockerignore && echo "server OK"</automated>
  </verify>
  <done>Both .dockerignore files exist and contain exclusion patterns for node_modules, dist, .env, and .git. Each file is at the root of its respective service directory (client/ and server/).</done>
</task>

</tasks>

<verification>
1. Both files exist at correct paths: `ls pv-calculator/client/.dockerignore pv-calculator/server/.dockerignore`
2. Both exclude all required categories (node_modules, dist, .env, .git)
3. Both include negation for .env.example
4. Neither file excludes Dockerfile
5. Context size verification (if Docker is available): `cd pv-calculator/client && tar -czf /tmp/ctx-client.tar.gz --exclude-from=.dockerignore . && du -sh /tmp/ctx-client.tar.gz` should show a small context (source files only, not hundreds of MB)
</verification>

<success_criteria>
- pv-calculator/client/.dockerignore exists with all required exclusion patterns
- pv-calculator/server/.dockerignore exists with all required exclusion patterns
- Neither file would allow .env, node_modules, dist, or .git into a Docker build context
- BUILD-03 requirement is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-context-safety/01-01-SUMMARY.md`
</output>
